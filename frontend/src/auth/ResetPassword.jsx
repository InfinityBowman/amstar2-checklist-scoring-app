// This should basically be a mirror of the VerifyEmail page for sending a code to
// reset password and then SignUp page to let user type in new password and confirm it.
export default function ResetPassword() {
  return <div>Reset Password Page</div>;
}
